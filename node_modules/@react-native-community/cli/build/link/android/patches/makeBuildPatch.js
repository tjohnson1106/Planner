'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
exports.default = makeBuildPatch;

var _normalizeProjectName = _interopRequireDefault(
  require('./normalizeProjectName')
);

function _interopRequireDefault(obj) {
  return obj && obj.__esModule ? obj : { default: obj };
}

/**
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @format
 */
function makeBuildPatch(name) {
  const normalizedProjectName = (0, _normalizeProjectName.default)(name);
  const installPattern = new RegExp(
    `(implementation|api|compile)\\w*\\s*\\(*project\\(['"]:${normalizedProjectName}['"]\\)`
  );
  return {
    installPattern,
    pattern: /[^ \t]dependencies {(\r\n|\n)/,
    patch: `    implementation project(':${normalizedProjectName}')\n`
  };
}
